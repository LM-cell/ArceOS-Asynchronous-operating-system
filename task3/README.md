# task3

该目录用于后续实践任务，结构与 `task1` 保持一致。

```text
task3/
├── data/
├── reports/
├── scripts/
├── src/
│   └── bin/
└── tests/
```

后续实现时建议继续按以下约定放置文件：

- `src/`：任务代码。
- `tests/`：测试代码。
- `scripts/`：运行和测试脚本。
- `data/`：输入数据。
- `reports/`：实验报告和结果分析。
